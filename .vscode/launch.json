{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Run with arguments",
            "program": "${workspaceFolder}/build/app/taskforce.app.exe",
            "cwd": "${workspaceFolder}"
        },
    ],
}