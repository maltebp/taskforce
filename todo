☐ Fix crash in CodeLLDB 
    It keeps crashing during debugging with some weird error.
    It seems to have been fixed before
    https://github.com/llvm/llvm-project/issues/56184

Serialization:
    ✔ Figure out design
    ✔ Simple JSON serializer
    ✔ Recap how I designed it
        ✔ Reconsider use of callback (e.g. WritableObject probably shouldn't use it)
    ✔ Add tests for optional variants
    ✔ Change return type of read
        ✔ Add result
    ☐ More types
        ☐ Double
        ☐ Float
    ☐ Error handling
    ☐ Cleanup
        ☐ No discard
        ☐ Split into headers
    Tests:
        Do parameterized tests? Test per type of serializ
        ☐ Each type
        ☐ Invalid input
        ☐ Invalid structure (custom serialization failing)

Workspace management:
    ☐ Open Workspace menu
    ☐ Save Workspace on key press (Ctrl+S)
    
Category column:
    ☐ Validation (from hardcoded values for now)
    ☐ Serialization

Summary:
    ☐ Get days in current week
    ☐ Summary page
        Totals:
            ☐ All categories (total time)
            ☐ Per category
            ☐ Missing hours
        Averages per day:
            ☐ All categories (total time)
            ☐ Per category
    ☐ Range
        ☐ Today
        ☐ This week

What I want for first usable prototype:
    Easily put in time entries for what I work on
        Put in description, start, date and end time
        Assign category (review,). These categories can be hardcoded for now
    Get overview of time spent
        Various ranges:.
            Today
            This week
        Metrics
            Total time spent
            Time spent on each category
        
    Category: may be inherited from task, but may be overwritten.
    Tasks are not part of first prototype


Backlog:

    Persistent state:
    These are not "settings", but state of program specific to PC (e.g. recently opened projects)
        ☐ Create API/system
        ☐ Tests
        ☐ Load
        ☐ Save
        ☐ Persistent state: re-open last project
    ☐ Allow custom summary ranges
    ☐ Make Ctrl+Enter create new row even if a row is selected
    parse_util:
        parse_hours_minutes:
            ☐ Do not parse hours if there is no colon
        parse_date:
            ☐ Auto input date if there is no year
    ☐ A cross-platform message box for asserts (not using ImGUI because it may not be initialized yet)
    ☐ Setup package manager
    
